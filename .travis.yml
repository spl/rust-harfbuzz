language: rust
cache: cargo
matrix:
  include:
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
script:
  - cargo package --manifest-path=harfbuzz-sys/Cargo.toml
