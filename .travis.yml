language: rust
cache: cargo
matrix:
  include:
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
    - os: linux
      rust: stable
script:
  - cargo package -vv --manifest-path=harfbuzz-sys/Cargo.toml
